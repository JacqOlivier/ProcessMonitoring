from setuptools import setup, find_packages

setup(
   name='processmonitoring',
   version='0.1',
   description='Used for a variety of process monitoring experiments.',
   author='Jacques Olivier',
   author_email='jacqueso@mintek.co.za',
   packages=find_packages()
)